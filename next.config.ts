import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'qmaosrvmdrtosfoxzzpk.supabase.co'  // Your Supabase project domain
    ]
  }
};

export default nextConfig;
